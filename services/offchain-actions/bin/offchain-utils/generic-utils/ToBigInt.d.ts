/**
 * @dev
 * @param {string | number | bigint} value
 * @returns {bigint}
 * Function to convert a string/number to a bigint, even if it has a decimal point.
 */
declare const toBigInt: (value: string | number | bigint) => bigint;
export default toBigInt;
