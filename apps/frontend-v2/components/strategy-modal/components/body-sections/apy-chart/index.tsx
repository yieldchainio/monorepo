/**
 * A chart showcasing a strategy's APY (Annual Percentage Yield) over time.
 */

import { InterModalSection } from "../../general/modal-section";

export const ApyChart = () => {
  return <InterModalSection height="h-[30%]"></InterModalSection>;
};
