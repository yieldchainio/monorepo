/**
 * TXN visuallization for sol
 */

const SolanaTxnsVisuallization = async () => {
  await new Promise((res) => setTimeout(() => res(true), 10000));

  return <div className=""></div>;
};

export default SolanaTxnsVisuallization;
